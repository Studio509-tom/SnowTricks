<div class="confirm-delete-partial">
	<p>Êtes vous sur de vouloir supprimer ce commentaire ?</p>
	<div class="btn-modal-delete">
		<button class="btn btn-light close-modal">Annuler</button>
		<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment.id) }}">
		<button class="btn btn-light confirm-delete" data-url="{{ path('app_ajax_comment_delete', {'id': comment.id}) }}" >Confirmer</button>
	</div>
</div>
